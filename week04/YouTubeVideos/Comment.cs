class Comment
{
    public string _name;
    public string _text;

    //got rid of the bare Comment class due to not seeing a need for it in this assignment

    public Comment(string name, string text)
    {
        _name = name;
        _text = text;
    }

    //didn't add a display function because this is about abstraction not encapsulation
}